# oberdiek -> atbegshi
# ms -> everyshi
# graphics -> graphicx
language: ruby
rvm:
- 2.2.5
install:
  - source travis/texlive/install
  - bundle install
script: 
  - bundle exec rake install
  - bundle exec rake "features[.*,tex]"
branches:
  only:
  - master
# sudo: false
