language: ruby
rvm:
- 2.1.2
before_script:
  - ./script/tl
  - export PATH=/tmp/texlive/bin/x86_64-linux:$PATH
  - tlmgr install standalone pgf oberdiek
  - tlmgr install latex-bin context
  - bundle install
script: 
  - bundle exec rake install
  - bundle exec rake features
branches:
  only:
  - master
# sudo: false
