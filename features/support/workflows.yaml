tex:
  dialect: tex
  pipeline: tex

# pdftex:
#   dialect: pdftex
#   pipeline: pdftex

# xetex:
#   dialect: xetex
#   pipeline: xetex

# luatex:
#   dialect: luatex
#   pipeline: luatex

latex:
  dialect: latex
  pipeline: latex

# pdflatex:
#   dialect: pdflatex
#   pipeline: pdflatex

# xelatex:
#   dialect: xelatex
#   pipeline: xelatex

# lualatex:
#   dialect: xelatex
#   pipeline: lualatex

context:
  dialect: context
  pipeline: context
