tex:
  - &tex
    - tex
    - <%= jobname %>.tex

xetex:
  - &xetex
    - xetex
    - <%= jobname %>.tex

latex:
  - &latex
    - latex
    - <%= jobname %>.tex

luatex:
  - &luatex
    - luatex
    - <%= jobname %>.tex

xelatex:
  - &xelatex
    - xelatex
    - <%= jobname %>.tex

lualatex:
  - &lualatex
    - lualatex
    - <%= jobname %>.tex

pdftex:
  - &pdftex
    - pdftex
    - <%= jobname %>.tex

pdflatex:
  - &pdflatex
    - pdflatex
    - <%= jobname %>.tex

context:
  - &context
    - context
    - <%= jobname %>.tex






dvipng:
  - &dvipng
    - convert
    - -density
    - "300"
    - <%= jobname %>.dvi
    - <%= jobname %>.png

pdfpng:
  - &pdfpng
    - convert
    - -density
    - "300"
    - <%= jobname %>.pdf
    - <%= jobname %>.png
